services:
  overleaf-mcp:
    image: ghcr.io/firefox2100/overleaf-mcp:latest
    restart: unless-stopped
    command: ["--http", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    env_file:
      - .env
